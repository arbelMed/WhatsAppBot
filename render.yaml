services:
  - type: web
    name: whatsapp-aesthetic-bot
    env: python
    plan: free
    buildCommand: "echo \"No build step needed\""
    startCommand: python bot.py
    envVars:
      - key: PORT
        value: 10000
